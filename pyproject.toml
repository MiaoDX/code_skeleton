[project]
name = "claude-devkit"
version = "0.2.0"
description = "Shared skills, commands, and update scripts for Claude Code and Codex"
requires-python = ">=3.12"
dependencies = [
    "markitdown",
]
