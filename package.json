{
  "name": "intuitive-flow",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test scripts/**/*.test.ts",
    "verify": "bun run check && bun run test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
