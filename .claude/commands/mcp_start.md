---
description: Enable a specific MCP server
---
To enable MCP server "$ARGUMENTS", the USER should type:

/mcp enable $ARGUMENTS

(This is a user-only interactive command, not executable by Claude)
