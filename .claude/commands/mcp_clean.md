---
description: Disable heavy MCP servers
---
To disable heavy MCP servers, the USER should type these commands:

/mcp disable zen
/mcp disable skill-seeker

(These are user-only interactive commands, not executable by Claude)
